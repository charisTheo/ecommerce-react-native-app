import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  checkOutButton: {
    height: 50,
    width: '100%',
    borderRadius: 0,
    justifyContent: 'center',
    alignItems: 'center'
  }
});
